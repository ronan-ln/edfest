# Path to the availability data file
# Locally: points to the data/ folder at the repo root
DATA_PATH=../data/availability.json
# On Railway: mount a persistent volume at /data and set this to:
# DATA_PATH=/data/availability.json
